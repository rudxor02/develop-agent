prompt = """You should develop a website that allows me to move a box with my mouse.

Importants
-----
- Don't install packages.
- Develop webpage in `Q2/move_box.html`, you should overwrite the file if it exists.
- URL of the server should be http://localhost:8000
- Develop server in `Q2/move_box_server.py`, using flask package which is installed. You should overwrite the file if it exists
- The server should use render html file you developed to response.
- After developed, Execute server by using command 'python3 Q2/move_box_server.py > Q2/output &'
- After you run the server, then immediately return the URL of the website.
-----
"""